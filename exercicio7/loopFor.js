/*
Crie um array de números de 1 a 5. 
Use um loop for para percorrer o array e exibir cada número no console.
*/

let numeros = [1, 2, 3, 4, 5];
for (let i = 0; i < numeros.length; i++) {
  console.log(numeros[i]);
}
